let word = "banana"
let resultado = "";
let tamanho = word.length - 1;
for (let index = tamanho; index <= tamanho; index -=1) {
    if (index >= 0) {
        resultado += word[index]
    }
    else {
        break
    }
}
console.log(resultado)

